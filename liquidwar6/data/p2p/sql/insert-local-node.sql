INSERT OR REPLACE INTO lw6_node (
       node_creation_timestamp,
       node_version,
       node_version,
       node_stamp,
       node_id,
       node_url,
       node_title,
       node_description,
       node_bench,
       ip,
       port
) VALUES (
       %d,
       '%s',
       '%s',
       %d,
       '%s',
       '%s',
       '%s',
       '%s',
       %d,
       '%s',
       %d
);

